def LinearSearch(list, item):
    position = 0
    found = False
        while position < len(List) and not found:
            if List[position] = item:
                found = True
            position += 1